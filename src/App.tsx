import DefaultLayout from './layouts/DefaultLayout';

/**
 * 애플리케이션의 메인 진입점 컴포넌트입니다.
 * DefaultLayout을 통해 전체적인 구조를 잡고, 그 안에 페이지 콘텐츠를 넣습니다.
 */
export default function App() {
  return (
    <DefaultLayout>
    </DefaultLayout>
  );
}
